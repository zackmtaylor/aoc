{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Day",
            "type": "shell",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "${workspaceFolder}/${input:day}/main.cpp",
                "-o",
                "${workspaceFolder}/${input:day}/main"
            ],
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "inputs": [
        {
            "id": "day",
            "description": "Day to build.",
            "type": "promptString"
        }
    ]
}